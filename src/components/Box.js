import React from 'react';
import '../styles/box.css';

const Input = (props) => {
    const {placeholder, type} = props;
    
    return(
        <div className="box">
            a
        </div>
    )
}
export default Input;
    
